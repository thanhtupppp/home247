# Workspace Rules

- **Firebase Integration Rule:** Always look for and use the appropriate Firebase agent skills (defined in [SKILL.md](file:///d:/home247/.agents/skills/firebase-agent-skills/SKILL.md)) to perform tasks related to Firebase (such as setting up Authentication, Firestore, or app registration).
- **Target Firebase Project:** Always use Firebase project `530902362759` for backend connectivity.
